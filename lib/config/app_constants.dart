class AppConstants {
  // ─── المسافات والهوامش (Padding & Margins) ───
  static const double paddingSmall = 8.0;
  static const double paddingNormal = 16.0;
  static const double paddingLarge = 24.0;

  // ─── الحواف الدائرية (Border Radius) ───
  static const double radiusSmall = 8.0;
  static const double radiusNormal = 12.0;
  static const double radiusLarge = 16.0;

  // ─── الارتفاعات (Heights) ───
  static const double buttonHeight = 48.0;
  static const double inputHeight = 56.0;

  // ─── أوقات الأنيميشن (Durations) ───
  static const int animationFast = 200; // milliseconds
  static const int animationNormal = 300; // milliseconds

  // ─── ثوابت أخرى ───
  static const int maxDecimals = 2; // تقريب الأرقام لخانين عشريتين
}