import 'package:flutter/material.dart';

class AppColors {
  // ─── الألوان الأساسية ──────────────────
  static const primary = Color(0xFF1565C0);       // أزرق داكن
  static const primaryLight = Color(0xFF1E88E5);
  static const secondary = Color(0xFF00897B);     // أخضر مزرق
  static const accent = Color(0xFFFF6F00);        // برتقالي للتنبيهات

  // ─── الخلفيات ─────────────────────────
  static const background = Color(0xFFF5F7FA);
  static const surface = Color(0xFFFFFFFF);
  static const cardBackground = Color(0xFFFFFFFF);

  // ─── حالات الفواتير والسندات والمناقلات ──────────
  static const draftColor = Color(0xFFFF8F00);       // برتقالي — مسودة
  static const draftBackground = Color(0xFFFFF8E1);
  static const issuedColor = Color(0xFF2E7D32);      // أخضر — مُخرجة
  static const issuedBackground = Color(0xFFE8F5E9);
  static const sentColor = Color(0xFF1565C0);        // أزرق — مُرسلة
  static const sentBackground = Color(0xFFE3F2FD);

  // ─── العملات ──────────────────────────
  static const sypColor = Color(0xFF6A1B9A);         // بنفسجي لليرة
  static const usdColor = Color(0xFF2E7D32);         // أخضر للدولار

  // ─── النصوص ───────────────────────────
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textHint = Color(0xFFBDBDBD);
  static const textOnPrimary = Color(0xFFFFFFFF);

  // ─── الحدود والفواصل ──────────────────
  static const divider = Color(0xFFE0E0E0);
  static const border = Color(0xFFBDBDBD);

  // ─── الحالات ──────────────────────────
  static const success = Color(0xFF2E7D32);
  static const error = Color(0xFFC62828);
  static const warning = Color(0xFFFF8F00);
  static const info = Color(0xFF1565C0);

  // ─── الهدايا ──────────────────────────
  static const giftBackground = Color(0xFFFCE4EC);
  static const giftBorder = Color(0xFFE91E63);
  static const giftText = Color(0xFFAD1457);

  // ─── شريط التنقل ──────────────────────
  static const navBarBackground = Color(0xFF1565C0);
  static const navBarSelected = Color(0xFFFFFFFF);
  static const navBarUnselected = Color(0xFF90CAF9);
}